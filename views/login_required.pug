extends layout

block page_name
  | Error

block content
  h1 Login required
  p This ThingEngine instance is configured to require login.
  p
    a(href='javascript:history.back()').btn.btn-default Go back
    span= " "
    a(href='/user/login').btn.btn-primary Log in
