# Defaults for thingengine-server initscript
# sourced by /etc/init.d/thingengine-server
# installed at /etc/default/thingengine-server by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
